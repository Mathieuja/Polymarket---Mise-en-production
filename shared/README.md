# app-shared

Shared package for Polymarket backend and worker applications.

Contains database configurations, models, schemas, and common utilities.

## Installation

```bash
pip install /path/to/shared
```
