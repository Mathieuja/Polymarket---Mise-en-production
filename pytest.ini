[pytest]
pythonpath = .
testpaths =
    tests
markers =
    unit: fast isolated unit tests
    integration: integration tests requiring multiple components/services
    component: tests focused on a specific application component
