# Core dependencies
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-dotenv>=1.0
python-jose[cryptography]>=3.3
boto3>=1.34
redis>=5.0.0

#maybe split in two files later one for dev and one for prod
# Dev / quality
ruff>=0.5
pytest>=8.2

# Shared package (relative path)
./shared

# Security
passlib[bcrypt]
bcrypt==4.0.1
