# Worker application for Polymarket data ingestion and processing
# Reuses shared package with backend

fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-dotenv>=1.0
boto3>=1.34
duckdb>=1.0

# Dev / quality
ruff>=0.5
pytest>=8.2

# Shared package (relative path)
./shared
